(ns elo100.core-test
  (:use clojure.test
        elo100.core
        minichess.core))

(deftest thing-test
  (testing "something"
    (is (= true true))))

;; (println "Ready")
;; (read-line)
;; (println "Go!")
;; (println (depth-limited-bot-move initial-game-state 4))
;; (println "Done~")
